Update Npm Packages.
1.  npm install -g npm-check-updates
2.  npm-check-updates
3.  ncu -a/--upgradeAll
4.  npm update --save

Run ncu with -u to upgrade package.json
  Example: ncu -u
Once the package.json is updated then simply run 'npm update'